# Anonimizzatore General Purpose
# Installa con: pip install -r requirements.txt

# Lettura/scrittura .docx
python-docx>=1.1.0

# Lettura PDF
pypdf>=4.0.0

# Lettura .xlsx / .ods (spreadsheet)
openpyxl>=3.1.0

# Profili YAML e settings
PyYAML>=6.0

# Lettura .odt / .ods (OpenDocument)
odfpy>=1.4.2

# Lettura .pptx (PowerPoint)
python-pptx>=0.6.23

# Lettura .rtf (Rich Text Format)
striprtf>=0.0.26

# Web UI (Chromebook/cross-platform)
flask>=3.0.0

# --- Solo per compila_modulo.py (funzione opzionale) ---

# Generazione PDF con campi modulo (AcroForm)
reportlab>=4.0.0

# Integrazione Gmail/Drive (OAuth2) — necessarie solo per --cerca-mail/--leggi-mail/
# --bozza-risposta e la ricerca su Drive; non richieste per --cerca-archivio o --dati/--output
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
